# PostgreSQL password — used by both postgres and pgadmin services
POSTGRES_PASSWORD=your_password_here

# MinIO credentials (root user must be 3–20 chars, password must be 8+ chars)
MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=your_password_here
